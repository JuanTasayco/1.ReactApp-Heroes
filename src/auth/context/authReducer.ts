
export const AuthReducer = (state = {}, action: any) => {
    switch (action.type) {
        case 'login': {
            break;
        }
        case 'logout': {
            break;
        }
        default: {
            return state;
        }
    }
}